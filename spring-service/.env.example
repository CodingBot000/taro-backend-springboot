# Spring Boot runtime
PORT=8080
APP_VERSION=local-dev
RATE_LIMIT_REQUESTS_PER_MINUTE=10
SPRING_PROFILES_ACTIVE=postgres

# Frontend origins allowed to call this API
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3100,https://heartsignal.cloud,https://www.heartsignal.cloud,https://taro.heartsignal.cloud

# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=taro
DB_USER=taro
DB_PASSWORD=change_me_before_production
DB_MAX_POOL_SIZE=5
DB_MIN_IDLE=1
DB_CONNECTION_TIMEOUT_MS=30000
SPRING_JPA_HIBERNATE_DDL_AUTO=none
SPRING_JPA_SHOW_SQL=false

# Hugging Face / Gradio
HF_SPACE_URL=AutoBot000/tarot-reading
HF_TOKEN=hf_replace_me
HF_API_PREFIX=/gradio_api
HF_CONNECT_TIMEOUT=5s
HF_READ_TIMEOUT=130s
HF_GENERATE_READING_API_NAME=generate_reading
HF_BACKEND_VERSION_API_NAME=backend_version

# Docker / compose helpers
HOST_PORT=8080
DOCKER_IMAGE_NAME=taro-spring-service:local
TZ=Asia/Seoul
JAVA_OPTS=-Xms256m -Xmx512m
